[package]
name = "spellbook"
version = "0.1.0"
authors = ["Michael Davis <mcarsondavis@gmail.com>"]
edition = "2021"
license = "MPL-2.0"
rust-version = "1.60"

[dependencies]
# Can we drop regex? Hunspell only seems to allow a subset of regex
# functionality like '^', '*', character groups and negative character
# groups.
regex = "1"
# TODO: investigate using smartstring to cut down on allocations since
# there are plenty of small strings.

[dev-dependencies]
xdg = "2.5"
